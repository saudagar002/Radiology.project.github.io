import React from "react";

const ViewerPage = () => {
  const fileName = "./svs_files.txt"; // Replace with your desired file name

  return (
    <div>
      <h2>Viewing SVS File: {fileName}</h2>
      {/* Add logic to display the SVS file based on the 'fileName' */}
    </div>
  );
};

export default ViewerPage;
